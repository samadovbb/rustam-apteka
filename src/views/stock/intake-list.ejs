<%- include('../partials/header') %>

<div class="main-wrapper">
    <%- include('../partials/sidebar') %>

    <main class="main-content">
        <div class="topbar">
            <h2><%= title %></h2>
            <div class="user-info">
                <span><%= lang.common.welcome %>, <strong><%= locals.user ? user.full_name || user.login : 'Admin' %></strong></span>
            </div>
        </div>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><%= lang.stockIntake.allIntakes %></h3>
                    <a href="/stock-intake/create" class="btn btn-primary"><%= lang.stockIntake.newIntake %></a>
                </div>

                <div class="table-responsive">
                    <table>
                        <thead>
                            <tr>
                                <th><%= lang.table.id %></th>
                                <th><%= lang.table.date %></th>
                                <th><%= lang.table.supplier %></th>
                                <th><%= lang.table.total %></th>
                                <th><%= lang.table.actions %></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (intakes.length === 0) { %>
                                <tr>
                                    <td colspan="5" class="text-center"><%= lang.stockIntake.notFound %></td>
                                </tr>
                            <% } else { %>
                                <% intakes.forEach(intake => { %>
                                    <tr>
                                        <td>#<%= intake.id %></td>
                                        <td><%= new Date(intake.intake_date).toLocaleDateString('ru-RU') %></td>
                                        <td><%= intake.supplier_name %></td>
                                        <td>$<%= parseFloat(intake.total_amount || 0).toFixed(2) %></td>
                                        <td>
                                            <a href="/stock-intake/<%= intake.id %>" class="btn btn-sm btn-primary"><%= lang.buttons.view %></a>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
</div>

<%- include('../partials/footer') %>
