<%- include('../partials/header') %>

<div class="main-wrapper">
    <%- include('../partials/sidebar') %>

    <main class="main-content">
        <div class="topbar">
            <h2><%= title %></h2>
            <div class="user-info">
                <span><%= lang.common.welcome %>, <strong><%= locals.user ? user.full_name || user.login : 'Admin' %></strong></span>
            </div>
        </div>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><%= lang.stockIntake.intakeDetails %></h3>
                    <div>
                        <button onclick="deleteIntake(<%= intake.id %>)" class="btn btn-danger" style="margin-right: 10px;">O'chirish</button>
                        <a href="/stock-intake" class="btn btn-secondary"><%= lang.common.backToList %></a>
                    </div>
                </div>

                <div style="margin-bottom: 20px;">
                    <p><strong><%= lang.table.id %>:</strong> #<%= intake.id %></p>
                    <p><strong><%= lang.stockIntake.supplier %>:</strong> <%= intake.supplier_name %></p>
                    <p><strong><%= lang.common.date %>:</strong> <%= new Date(intake.intake_date).toLocaleDateString() %></p>
                    <p><strong><%= lang.stockIntake.totalAmount %>:</strong> $<%= parseFloat(intake.total_amount || 0).toFixed(2) %></p>
                    <% if (intake.notes) { %>
                        <p><strong><%= lang.common.notes %>:</strong> <%= intake.notes %></p>
                    <% } %>
                </div>

                <div class="table-responsive">
                    <table>
                        <thead>
                            <tr>
                                <th><%= lang.table.product %></th>
                                <th><%= lang.table.barcode %></th>
                                <th><%= lang.table.quantity %></th>
                                <th><%= lang.table.purchasePrice %></th>
                                <th><%= lang.table.subtotal %></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% items.forEach(item => { %>
                                <tr>
                                    <td><%= item.product_name %></td>
                                    <td><%= item.barcode %></td>
                                    <td><%= item.quantity %></td>
                                    <td>$<%= parseFloat(item.purchase_price || 0).toFixed(2) %></td>
                                    <td>$<%= parseFloat(item.subtotal || 0).toFixed(2) %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
</div>

<%- include('../partials/footer') %>

<script>
async function deleteIntake(id) {
    const result = await Swal.fire({
        title: 'Ishonchingiz komilmi?',
        text: 'Bu qabulni o\'chirishni xohlaysizmi? Bu harakat ombor inventarizatsiyasini kamaytiradi va bekor qilib bo\'lmaydi!',
        icon: 'warning',
        showCancelButton: true,
        confirmButtonText: 'Ha, o\'chirish',
        cancelButtonText: 'Bekor qilish',
        confirmButtonColor: '#d33',
        cancelButtonColor: '#3085d6'
    });

    if (result.isConfirmed) {
        try {
            const response = await fetch(`/stock-intake/${id}`, { method: 'DELETE' });
            const data = await response.json();
            if (data.success) {
                Swal.fire('O\'chirildi!', data.message, 'success')
                    .then(() => window.location.href = '/stock-intake');
            } else {
                Swal.fire('Xato!', data.error, 'error');
            }
        } catch (error) {
            Swal.fire('Xato!', 'O\'chirishda xatolik yuz berdi', 'error');
        }
    }
}
</script>
