<%- include('../partials/header') %>

<div class="main-wrapper">
    <%- include('../partials/sidebar') %>

    <main class="main-content">
        <div class="topbar">
            <h2><%= title %></h2>
            <div class="user-info">
                <span><%= lang.common.welcome %>, <strong><%= locals.user ? user.full_name || user.login : 'Admin' %></strong></span>
            </div>
        </div>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><%= lang.suppliers.title %></h3>
                    <a href="/suppliers/create" class="btn btn-primary"><%= lang.suppliers.addNew %></a>
                </div>

                <div class="table-responsive">
                    <table>
                        <thead>
                            <tr>
                                <th><%= lang.table.id %></th>
                                <th><%= lang.table.name %></th>
                                <th><%= lang.table.phone %></th>
                                <th><%= lang.table.email %></th>
                                <th><%= lang.table.address %></th>
                                <th><%= lang.table.actions %></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (suppliers.length === 0) { %>
                                <tr>
                                    <td colspan="6" class="text-center"><%= lang.suppliers.noSuppliers %></td>
                                </tr>
                            <% } else { %>
                                <% suppliers.forEach(supplier => { %>
                                    <tr>
                                        <td>#<%= supplier.id %></td>
                                        <td><%= supplier.name %></td>
                                        <td><%= supplier.phone || '-' %></td>
                                        <td><%= supplier.email || '-' %></td>
                                        <td><%= supplier.address || '-' %></td>
                                        <td>
                                            <a href="/suppliers/<%= supplier.id %>/edit" class="btn btn-sm btn-secondary"><%= lang.buttons.edit %></a>
                                            <form action="/suppliers/<%= supplier.id %>/delete" method="POST" style="display: inline;">
                                                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('<%= lang.common.confirmDelete %>')"><%= lang.buttons.delete %></button>
                                            </form>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
</div>

<%- include('../partials/footer') %>
