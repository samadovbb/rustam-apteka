<%- include('../partials/header') %>

<div class="main-wrapper">
    <%- include('../partials/sidebar') %>

    <main class="main-content">
        <div class="topbar">
            <h2><%= title %></h2>
            <div class="user-info">
                <span><%= lang.common.welcome %>, <strong><%= locals.user ? user.full_name || user.login : 'Admin' %></strong></span>
            </div>
        </div>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><%= seller.full_name %> - <%= lang.sellers.currentInventory %></h3>
                    <a href="/sellers" class="btn btn-secondary"><%= lang.common.backToList %></a>
                </div>

                <div class="table-responsive">
                    <table>
                        <thead>
                            <tr>
                                <th><%= lang.table.product %></th>
                                <th><%= lang.table.barcode %></th>
                                <th><%= lang.table.quantity %></th>
                                <th><%= lang.table.sellerPrice %></th>
                                <th><%= lang.table.totalValue %></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (inventory.length === 0) { %>
                                <tr>
                                    <td colspan="5" class="text-center"><%= lang.sellers.noInventory %></td>
                                </tr>
                            <% } else { %>
                                <% inventory.forEach(item => { %>
                                    <tr>
                                        <td><%= item.product_name %></td>
                                        <td><%= item.barcode %></td>
                                        <td><%= item.quantity %></td>
                                        <td>$<%= parseFloat(item.seller_price || 0).toFixed(2) %></td>
                                        <td>$<%= (parseFloat(item.seller_price || 0) * parseInt(item.quantity || 0)).toFixed(2) %></td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
</div>

<%- include('../partials/footer') %>
